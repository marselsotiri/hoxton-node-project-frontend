import React from 'react';
import SignUpForm from '../../Components/SignUpForm';

const SignUp = () => {
    return (
        <section className='log_in'>
            <h2>Sign up via Phone or Email</h2>
           <SignUpForm/>
        </section>
    );
};

export default SignUp;
